1) WS: Refactoring- Use DAO pattern correctly! Also include controllers
2) FE: Fix own style loading in Firefox
3) DB: Add constraint on networks- there shouldn't be multiple networks with the same name for one user.